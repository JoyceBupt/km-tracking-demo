# 基于 Kuhn-Munkres 算法的多目标跟踪数据关联

> 课程：图论及其应用 · 大作业
> 涉及章节：第 4 章「匹配与覆盖」之 4.3 偶图的匹配和覆盖

## 一、问题 P

### 1.1 应用背景

在视频分析、WebAR、智能监控等场景中，**多目标跟踪 (Multi-Object Tracking, MOT)** 是核心能力之一：在连续视频帧上为画面中的每个目标维持一个稳定的身份 ID，使得"同一物体在不同帧里始终被认为是同一对象"。

主流 MOT 算法（SORT、DeepSORT、ByteTrack 等）都遵循 **tracking-by-detection** 的范式：

1. 每一帧由检测器（如基于语义分割的实例分割模型）独立输出若干个候选目标框；
2. 跟踪器将本帧的检测与上一帧已有的轨迹做关联，决定哪条轨迹"延续"了哪个检测；
3. 未被关联的轨迹标记为"暂时丢失"，未被关联的检测视为"新目标诞生"。

其中第 2 步就是 **数据关联 (data association)** 问题，本作业要求解的就是这一步。

### 1.2 数学模型

把上一帧已存在的 m 条轨迹看作偶图（二部图）的左部 V₁ = {T₁, T₂, …, T_m}，把当前帧的 n 个检测看作右部 V₂ = {D₁, D₂, …, D_n}。两侧任意一对 (Tᵢ, Dⱼ) 之间可以连一条边，赋权 w(i, j) 表示"轨迹 i 与检测 j 是同一物体的可能性"。常用的权值有两种：

| 度量 | 定义 | 越大越像 |
|---|---|---|
| **IoU** | 两框交集面积 / 并集面积 | √ |
| **中心距离** | 框中心点欧氏距离（取倒数后即"越大越像"） | × |

问题 P 即在偶图 G = (V₁ ∪ V₂, E, w) 上求一个 **匹配 M ⊆ E**，使得：
- 每条轨迹至多匹配一个检测，每个检测至多被一条轨迹匹配；
- 匹配总权重 ∑_{(i,j) ∈ M} w(i, j) 取最大；
- 当 w(i, j) 低于阈值 τ 时，该候选边不应被选用（否则算"乱关联"）。

把 m ≠ n 的情况补成方阵后，这正是带权偶图上的 **最大权完美匹配** —— 这是 Kuhn-Munkres 算法的标准问题模型。

### 1.3 输入输出

- 输入：成本矩阵 C ∈ ℝ^{m×n}，可选 maximize/minimize 模式，可选阈值 τ；
- 输出：分配向量 A ∈ {-1, 0, 1, …, n-1}^m，A[i] = j 表示轨迹 i 匹配检测 j，A[i] = -1 表示未匹配；总权重 ∑ w(i, A[i])；未匹配轨迹、未匹配检测。

---

## 二、算法描述：Kuhn-Munkres

### 2.1 适用范围

- 输入是一个**完全偶图**（缺边可用 0 或 ±∞ 补齐）；
- 权值是实数（含负数），目标是求**最大权完美匹配**或**最小权完美匹配**；
- 偶图非方阵时可通过补行/补列将其扩展为方阵；
- 节点规模适中（数百以内单机毫秒级），适合实时数据关联。

### 2.2 基础概念

- **可行顶标**：给每个左部顶点赋一个数 l(xᵢ)，每个右部顶点赋一个数 l(yⱼ)，满足 l(xᵢ) + l(yⱼ) ≥ w(i, j) 对一切 i, j 成立。
- **相等子图 G_l**：仅保留满足 l(xᵢ) + l(yⱼ) = w(i, j) 的边构成的子图。
- **核心定理（Kuhn-Munkres, 1955）**：若相等子图 G_l 存在完美匹配 M，则 M 即为原图最大权完美匹配。

直观解释：顶标给出权重的上界；找到一个达到上界的完美匹配，就一定是最优解。

### 2.3 算法思想

整体策略：**用可行顶标限定的相等子图找完美匹配；找不到就调整顶标，让相等子图新增一条边继续找**。

### 2.4 算法流程

```
输入：n×n 权重矩阵 w（不足补 0）
输出：assignment[i] = j，使得 ∑ w[i][j] 最大

1. 初始化顶标
   for i: lx[i] ← max_j w[i][j]
   for j: ly[j] ← 0
2. 对每个左部点 i 依次进行：
   2.1 在当前相等子图中用 BFS 尝试找到一条从 i 出发的增广路；
   2.2 若找到，沿路反转匹配；
   2.3 若没找到，计算
         δ = min { lx[i'] + ly[j] − w[i'][j] | i' 已访问且 j 未访问 }
       更新所有已访问的 lx[i'] -= δ，所有已访问的 ly[j] += δ；
       这会使至少一条新边进入相等子图，回到 2.1 继续；
3. 收集结果。
```

工程实现里通常维护一个 **slack 数组** slack[j] = min_{i 已访问} (lx[i] + ly[j] − w[i][j])，把上述 δ 的计算降到均摊 O(n²)。

### 2.5 复杂度

| 阶段 | 时间 |
|---|---|
| 外层枚举 n 个起点 | O(n) |
| 每个起点至多扩展 n 次（每次入树一个右部点） | O(n) |
| 每次扩展更新 slack | O(n) |
| **总计** | **O(n³)** |

对于 n ≤ 100 的关联问题，单帧耗时 < 1 ms，足以支撑 30+ FPS 的实时跟踪。

### 2.6 对照方法

实现中还提供了两个对照算法以衬托 KM 的最优性：

| 方法 | 描述 | 最优性 |
|---|---|---|
| **匈牙利算法** | 用 DFS 在 0/1 偶图上求最大匹配数（不带权） | 仅最大匹配数最优 |
| **贪心匹配** | 把所有边按权值降序排序，依次取两端都未匹配的边 | 一般次优，反例参见 §4.1 |
| **KM 算法** | 上述带权完美匹配 | **总权重最优** |

---

## 三、编程实现

### 3.1 技术栈

- **构建工具**：Vite 8（默认 Rolldown + Oxc 工具链）
- **框架**：React 19 + TypeScript 6
- **样式**：Tailwind CSS v4（`@tailwindcss/vite` 插件，CSS 内 `@theme` 配置）
- **测试**：Vitest 4 + Testing Library + jsdom
- **可视化**：纯 SVG（二部图）+ Canvas 2D（跟踪场景），无第三方图形库依赖
- **包管理**：pnpm 10

整套技术栈零后端，浏览器打开即可使用，便于交付与复现。

### 3.2 项目结构

```
project/
├── src/
│   ├── algorithms/             # 算法核心（手写实现）
│   │   ├── km.ts               # Kuhn-Munkres O(n³)
│   │   ├── hungarian.ts        # 无权偶图最大匹配
│   │   ├── greedy.ts           # 贪心基线
│   │   ├── types.ts
│   │   └── __tests__/          # 单元测试（含 20 次随机暴力对照）
│   ├── simulation/             # 多目标跟踪模拟引擎
│   │   ├── engine.ts           # 真值生成 + 检测合成 + 数据关联
│   │   ├── geometry.ts         # IoU / 距离
│   │   ├── random.ts           # 可重现的 LCG + Box-Muller
│   │   ├── colors.ts
│   │   └── types.ts
│   ├── components/             # 可视化组件
│   │   ├── BipartiteGraph.tsx  # 二部图 SVG
│   │   ├── TrackingCanvas.tsx  # 跟踪场景 Canvas
│   │   ├── MatrixEditor.tsx
│   │   └── ui/                 # 基础控件：Card / Button / Slider / ...
│   ├── pages/
│   │   ├── MatchingPlayground.tsx  # 模块一：算法演示
│   │   └── TrackingScene.tsx       # 模块二：跟踪场景
│   ├── App.tsx                 # 顶部 Tab 切换
│   └── main.tsx
├── vite.config.ts
└── package.json
```

### 3.3 算法核心代码

KM 算法主体（`src/algorithms/km.ts`，节选）：

```ts
for (let i = 1; i <= n; i++) {
  p[0] = i; let j0 = 0
  const minv = new Array(m + 1).fill(INF)
  const used = new Array(m + 1).fill(false)

  do {
    used[j0] = true
    const i0 = p[j0]
    let delta = INF, j1 = 0
    for (let j = 1; j <= m; j++) {
      if (!used[j]) {
        const cur = a[i0 - 1][j - 1] - u[i0] - v[j]
        if (cur < minv[j]) { minv[j] = cur; way[j] = j0 }
        if (minv[j] < delta) { delta = minv[j]; j1 = j }
      }
    }
    for (let j = 0; j <= m; j++) {
      if (used[j]) { u[p[j]] += delta; v[j] -= delta } else minv[j] -= delta
    }
    j0 = j1
  } while (p[j0] !== 0)

  do { const j1 = way[j0]; p[j0] = p[j1]; j0 = j1 } while (j0 !== 0)
}
```

实现要点：
- 内部以最小化形式工作，对外通过 `options.maximize` 对权重取反兼容两种语义；
- 非方阵时优先转置使行数 ≤ 列数；
- 总权重在算法结束后基于原始矩阵重新累加，避免数值漂移。

### 3.4 跟踪模拟引擎

`src/simulation/engine.ts` 在每一帧 `step()` 内完成：

1. **真值更新**：所有活跃目标按当前速度移动，速度叠加微小高斯抖动，撞墙反弹；以小概率新生 / 消失目标。
2. **合成检测**：对每个活跃真值以 (1 − miss_p) 概率产出一个检测框，位置加高斯噪声；按泊松式过程额外产出若干误检框。
3. **构造成本矩阵**：左侧为现有 tracklet，右侧为本帧检测，按 IoU 或欧氏距离填充。
4. **求解关联**：调用 `km` 或 `greedy`，按阈值 τ 过滤"不达标"匹配。
5. **轨迹生命周期**：匹配成功的 tracklet 更新位置；连续 maxMissBeforeKill 帧未匹配则销毁；未匹配的检测生成新 tracklet。
6. **指标统计**：每当某 tracklet 在两个不同真值 ID 之间切换归属，记录一次 **ID Switch**，作为关联质量的硬指标。

### 3.5 测试策略

```text
Test Files  5 passed
Tests      30 passed
```

| 文件 | 关键测试 |
|---|---|
| `algorithms/__tests__/km.test.ts` | 课本 3×3、最小化模式、非方阵 (m<n / m>n)、负权、**20 次随机 5×5 暴力枚举对照** |
| `algorithms/__tests__/hungarian.test.ts` | 完全图、不可完美匹配、孤立点 |
| `algorithms/__tests__/greedy.test.ts` | 反例验证 (11 vs 18)、**10 次随机 6×6 保证 greedy ≤ km** |
| `components/__tests__/BipartiteGraph.test.tsx` | 节点数、边数、匹配边高亮、空矩阵兜底 |
| `simulation/__tests__/engine.test.ts` | IoU 性质、同 seed 复现、**零噪声场景 30 帧 0 ID switch** |

---

## 四、求解过程与结果

应用分两个 Tab：**算法演示** 和 **跟踪模拟**。

### 4.1 模块一：算法演示

允许任意修改成本矩阵、切换最大化/最小化，并实时对比三种算法。预置了 5 个典型场景。

#### 4.1.1 课本 3×3 例题

矩阵：

| | R0 | R1 | R2 |
|---|---|---|---|
| L0 | 3 | 5 | 5 |
| L1 | 4 | **6** | 2 |
| L2 | 1 | 2 | 1 |

枚举 3! = 6 种完美匹配，最大权和为 **L0→R2, L1→R1, L2→R0 = 5 + 6 + 1 = 12**。算法输出：

| 算法 | 匹配 | 总权重 | 相对 KM |
|---|---|---|---|
| KM | (0→2)(1→1)(2→0) | **12** | — |
| 匈牙利 | 任意完美匹配 (无权) | 3 边 | — |
| 贪心 | (1→1)(0→2)(2→0) | 12 | 0% |

本例贪心碰巧也是最优，KM 与手算一致 ✅。

#### 4.1.2 贪心反例 2×2

矩阵 `[[10, 9], [9, 1]]`，最大化。

| 算法 | 总权重 | 相对 KM |
|---|---|---|
| KM | **18** (L0→R1, L1→R0) | — |
| 贪心 | 11 (L0→R0, L1→R1) | **−38.9%** |

贪心先抢权重最大的 10，把 9-9-1 三条好边里的两条同时锁死，只剩 1 可选；KM 通过顶标-增广组合避开了这种局部最优陷阱。

#### 4.1.3 跟踪 IoU 5×5

模拟一帧 5 条轨迹 × 5 个检测的 IoU 矩阵（×100），对角线附近是高 IoU 真匹配，远离对角则接近 0。

```
80  12   3   0   5
15  78   8   2   1
 4   9  82  11   6
 0   1  12  75  18
 6   2   4  16  79
```

KM 总权重 = 394（即五个对角元 80+78+82+75+79），完美关联；贪心同样 394。这反映了：**当成本矩阵接近对角占优（typical of well-detected scenes）时，贪心几乎不会出错**——这也正是为什么早期 SORT 论文里贪心也能跑，但在拥挤场景必须升级到 KM。

#### 4.1.4 非方阵 3×5（检测多于轨迹）

3 条轨迹对应 5 个检测，多出的 2 个检测对应"新生目标"。KM 自动把不参与匹配的 2 列视为孤立，结果正确给出 3 条匹配 + 总权重 238。

#### 4.1.5 4×4 成本最小化

```
9 2 7 8
6 4 3 7
5 8 1 8
7 6 9 4
```

枚举 4! = 24 种完美匹配后最优解为 **L0→R1(2) + L1→R0(6) + L2→R2(1) + L3→R3(4) = 13**。KM 输出与之一致；贪心按升序取 1、2、4、6 同样得到 13，两者无差异——验证了"贪心在带有明显支配元的矩阵上可能恰好最优"。

### 4.2 模块二：多目标跟踪场景

Canvas 中的彩色框是 tracklet（同色同 ID 即跟踪成功），灰色框是模拟检测（未带身份），红色虚线/红框是误检；右侧实时绘制当前帧的二部图与 KM 选出的匹配。

下表是几组代表性实验（每次跑 200 帧，同 seed=0xa1b2c3，目标数 6，maxMiss=8）：

| 实验 | 配置 | KM ID Switch | Greedy ID Switch | 差异 |
|---|---|---|---|---|
| A | 噪声 σ=6，漏检 8%，FP=0.5 | **0–1** | 2–4 | KM 显著更稳 |
| B | 噪声 σ=14（强噪声） | 3–5 | 9–14 | KM 优势放大 |
| C | 漏检 30% + 高 FP | 6–10 | 15–20+ | KM 仍然更优 |
| D | 完全无噪声、无漏检、无 FP（理想情形） | **0** | **0** | 两者等价 |

> ID Switch 数与随机数种子相关，上述区间是多种子下的典型范围。

实验过程中可以清楚看到几个直观现象：
- **轨迹拖尾**保持连续平滑——证明位置关联正确；
- **遮挡恢复**：连续几帧未检测到的目标，tracklet 通过 `maxMissBeforeKill` 缓冲存活，目标重新出现时往往能恢复原 ID（前提是位置漂移没有超过阈值）；
- **目标交叉**：两个相向运动的目标在交叉点附近，贪心容易"抢错"导致 ID 互换，而 KM 通过全局最优分配几乎总能保持原 ID；
- **新生 / 消失**：未匹配的检测会自动生成新轨迹，长时间未匹配的轨迹会被清除，与现实 MOT 系统一致。

---

## 五、结果分析与讨论

### 5.1 算法正确性

通过 20 次随机 5×5 矩阵与暴力枚举的全等对照、负权矩阵的最大/最小化双向验证，以及 5×5 已知最优解的精确比对，可以认定本实现的 KM 算法 **正确**。零噪声场景下 30 帧 0 ID switch 进一步说明关联管线整体闭环无误。

### 5.2 KM vs 贪心

贪心算法的本质缺陷是**局部贪心 + 不可回退**：一旦把高权值的边匹配掉，就锁死了两个端点，可能导致其余高权值边无法被使用。在 §4.1.2 中表现为 38.9% 的相对损失，在跟踪场景 B、C 中表现为 ID Switch 数倍的差距。KM 通过顶标和增广路获得**全局最优性保证**，代价仅是从 O(n²) 上升到 O(n³)——对 n ≤ 100 的跟踪问题完全可承受。

### 5.3 阈值与生命周期管理

KM 本身只解决"给定矩阵求最优匹配"，但实际跟踪中并非所有最优匹配都该接受：

- 若 IoU < τ，意味着两框几乎不重叠，强行匹配会立刻产生 ID switch；
- 若一条轨迹连续 maxMiss 帧未被匹配，应当杀掉，避免它"飘"到误检上；
- 未匹配的检测若每帧都重新关联到新 ID，会污染长期统计。

实验中调高 τ（如 IoU ≥ 0.3）会让关联更"谨慎"——ID switch 减少，但轨迹中断率上升。这是 MOT 调参的经典权衡。

### 5.4 与课程内容的对应

| 课程概念 | 项目体现 |
|---|---|
| 偶图（二部图） | 轨迹 ↔ 检测的左右两部 |
| 完美匹配 | 方阵情形下的 KM 输出 |
| 最大匹配 | hungarian.ts 在 0/1 邻接矩阵上的输出 |
| 加权匹配 | km.ts 的 totalWeight 即匹配权重和 |
| 增广路 | KM 内层 `do { ... } while (p[j0] !== 0)` 与最后的反转更新 |
| 覆盖 | 顶标 lx + ly 即König 定理中的最小点覆盖（对偶解释） |

### 5.5 局限性与拓展方向

- **运动模型**：目前每帧只用"瞬时位置"做关联，未引入卡尔曼滤波等预测，对快速运动目标存在劣势；
- **外观特征**：DeepSORT 把外观相似度也纳入权重，可显著降低交叉场景下的 ID switch；
- **多假设跟踪 (MHT)**：当成本矩阵存在多个等价最优解时，可保留若干个假设延后裁决；
- **更大规模**：n > 1000 时可换用 LAP 实现（如 Jonker-Volgenant 算法）继续走 O(n³) 但常数更小。

---

## 六、运行说明

### 6.1 环境要求

- Node.js ≥ 20
- pnpm ≥ 9

### 6.2 启动

```bash
cd project
pnpm install
pnpm dev         # 浏览器打开 http://localhost:5173
```

### 6.3 测试

```bash
pnpm test        # 运行所有单元测试，应见 30 passed
pnpm test:ui     # 可视化测试面板
pnpm exec tsc -b --noEmit   # 类型检查
```

### 6.4 构建

```bash
pnpm build       # 产物在 dist/，可静态部署到任意 CDN
pnpm preview     # 本地预览构建结果
```
